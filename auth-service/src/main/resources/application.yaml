server:
  port: 8081

spring:
  application:
    name: auth-service
  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: super-secret-for-demo-only-change-in-prod
  expiration: 3600000   # 1 hour

logging:
  level:
    com.corebank: DEBUG
    org.springframework: INFO

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
